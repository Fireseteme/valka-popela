{
  "name": "valka-popela",
  "version": "0.3.0",
  "description": "Válka popela — multiplayer server (Node.js + WebSocket)",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js"
  },
  "dependencies": {
    "ws": "^8.21.3"
  }
}
